<%= form_with(model: room) do |form| %>
  <% if room.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
        <% room.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :senderid, style: "display: block" %>
    <%= form.text_field :senderid %>
  </div>

  <div>
    <%= form.label :receiverid, style: "display: block" %>
    <%= form.text_field :receiverid %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
